name: Sync with Bitbucket
on:
  workflow_dispatch:
  schedule:
    - cron: '0 0 * * *'
  
jobs:
  sync:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout GitHub repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Configure Git
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'

      - name: Sync from Bitbucket
        run: |
          git remote add bitbucket https://bitbucket.org/spekpy/spekpy_release.git
          git fetch bitbucket --verbose
          # Se utiliza 'master' ya que es la rama detectada en Bitbucket
          git merge bitbucket/master --allow-unrelated-histories --no-edit
          # Se utiliza 'master' para coincidir con tu rama actual en GitHub
          git push origin master
